# Servidores MCP (Model Context Protocol)

Este documento registra as configurações de MCP que eram utilizadas no ambiente do Antigravity, para que possam ser recriadas no Claude Code.

## > TODO: Confirmar com o autor
Não foram encontrados arquivos de configuração local (`.agents/mcp.json` ou similar) apontando para servidores MCP ativos de forma clara no repositório.
Por favor, se haviam integrações de MCP (ex: `postgres`, `github`, `supabase`), insira-as manualmente aqui utilizando o comando equivalente no Claude Code:

### Comandos de Referência (Claude Code)
Para cada servidor MCP listado acima, rode o comando abaixo no Claude Code substituindo os valores:
`claude mcp add <nome-do-servidor> <comando/url>`
(Lembre-se de fornecer os tokens via variáveis de ambiente seguras).
